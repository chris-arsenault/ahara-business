@AGENTS.md

## Claude-specific

- Use `feature-start` for architecture-level planning before implementation.
- Use `plan-phase` to expand one milestone into execution-ready steps.
- Use `repo-docs` for documentation surface changes, ADRs, and backlog updates.
