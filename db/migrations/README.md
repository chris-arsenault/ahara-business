# Migrations

Platform PostgreSQL migration directory for the `ahara-business` database.

M0 registers the database and leaves this directory intentionally empty. M2 adds
schema migrations, rollback files, and idempotent seed data for the mail model.
